execution_engine: asyncio
logger:
  transports: [console] # You can use [file, console] for both
  level: debug
  path: "logs/mcp-agent.jsonl" # Used for file transport
  # For dynamic log filenames:
  path_settings:
    path_pattern: "logs/mcp-agent-{unique_id}.jsonl"
    unique_id: "timestamp"  # Or "session_id"
    timestamp_format: "%Y%m%d_%H%M%S"

mcp:
  servers:
    fetch:
      command: "uvx"
      args: ["mcp-server-fetch"]
    filesystem:
      command: "npx"
      args:
        [
          "-y",
          "@modelcontextprotocol/server-filesystem",
          "/Users/macintoshi/Desktop/code/new_one_mcp/python",
        ]

openai:
  # Secrets (API keys, etc.) are stored in an mcp_agent.secrets.yaml file which can be gitignored
  default_model: gpt-4o,
  OPENAI_API_KEY: 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
